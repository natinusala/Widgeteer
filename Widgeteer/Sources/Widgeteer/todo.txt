réessayer les modifiers
restaurer hot restart
restaurer les preview
TODO: supprimer SIGUSR1 et watch le fichier plutôt
TODO: séparer Properties et Parameters de persistent object, dire que Parameter peut correspondre à une property via son nom pour pas dupliquer (mais ça peut aussi être un param seul) - permet de sortir init_type par exemple, qui va override la property
